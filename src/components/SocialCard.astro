---
const { url, name, target } = Astro.props;
---

<a href={url} target={target}>{name}</a>

<style>
    a {
        width: 100%;
        padding: 30px;
        background-color: var(--bg-color);
        border: 1px solid var(--border-color);
        border-radius: 5px;
    }
</style>
