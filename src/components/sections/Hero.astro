---
import { t } from "i18n:astro";
import { socialData } from "@/constants/socialData";
import { MailIcon, GithubIcon, LinkedinIcon } from "@lucide/astro";
---

<section class="hero">
    <div class="hero-avatar" aria-hidden="true">
        <img
            src="https://avatars.githubusercontent.com/u/121297520?v=4"
            alt=""
            loading="eager"
        />
    </div>

    <div class="hero-content">
        <div class="name-row">
            <h1 class="name">
                {t("hero:name")}
            </h1>
        </div>

        <p class="role">{t("hero:role")}</p>

        <p class="role-meta">
            <span set:html={t("hero:meta_line_1")} />
            <br />
            <span set:html={t("hero:meta_line_2")} />
        </p>

        <div class="hero-contact">
            <a href={socialData.email}>
                <MailIcon size={14} class="ci" />
                <span>{socialData.email.replace("mailto:", "")}</span>
            </a>
            <a href={socialData.github} target="_blank" rel="noopener noreferrer">
                <GithubIcon size={14} class="ci" />
                <span>GitHub</span>
            </a>
            <a href={socialData.linkedin} target="_blank" rel="noopener noreferrer">
                <LinkedinIcon size={14} class="ci" />
                <span>LinkedIn</span>
            </a>
        </div>
    </div>
</section>

<style>
    .hero {
        width: 100%;
        padding-top: 16px;
        margin-bottom: 72px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 32px;
        text-align: center;
    }

    .hero-content {
        min-width: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .hero-avatar {
        width: 130px;
        height: 130px;
        border-radius: 9999px;
        overflow: hidden;
        flex-shrink: 0;
    }

    .hero-avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
    }

    .name-row {
        display: grid;
        grid-template-columns: 1fr auto;
        gap: 20px;
        align-items: baseline;
        margin-bottom: 10px;
    }

    .name {
        font-size: clamp(30px, 4vw, 42px);
        font-weight: 600;
        letter-spacing: -0.03em;
        line-height: 1.1;
        color: var(--ink);
    }

    .role {
        font-size: clamp(16px, 1.8vw, 19px);
        color: var(--accent);
        font-weight: 500;
        letter-spacing: -0.015em;
        line-height: 1.35;
        margin-bottom: 32px;
    }

    .role-meta {
        font-size: 15px;
        color: var(--ink-soft);
        max-width: 56ch;
        line-height: 1.65;
    }

    .role-meta :global(strong) {
        color: var(--ink);
        font-weight: 600;
    }

    .hero-contact {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 12px 28px;
        margin-top: 32px;
        font-size: 14.5px;
        font-weight: 500;
    }

    .hero-contact a .ci {
        width: 16px;
        height: 16px;
    }

    .hero-contact a {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        color: var(--ink-soft);
        transition: color 0.2s ease;
    }

    .hero-contact a:hover {
        color: var(--accent);
    }

    .hero-contact a .ci {
        color: var(--ink-mute);
        transition: color 0.2s ease;
        flex-shrink: 0;
    }

    .hero-contact a:hover .ci {
        color: var(--accent);
    }

    @media (max-width: 768px) {
        .hero {
            gap: 28px;
        }
        .hero-avatar {
            width: 96px;
            height: 96px;
        }
    }

    @media (max-width: 600px) {
        .hero {
            padding-top: 8px;
            margin-bottom: 48px;
        }
        .hero-contact {
            gap: 10px 20px;
            font-size: 13px;
        }
    }
</style>
